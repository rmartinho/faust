file = _{ SOI ~ string+ ~ EOI }

string = { key ~ value }

key = @{ "{" ~ ident ~ "}" }
value = @{ (!"{" ~ ANY)+ }

ident = @{ (ASCII_ALPHANUMERIC | "_" | "-")+ }

COMMENT = _{ "¬" ~ (!NEWLINE ~ ANY)* ~ (NEWLINE | EOI) }
WHITESPACE = _{ " " | "\t" | NEWLINE }
